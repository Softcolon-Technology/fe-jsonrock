name: Next.js CI/CD for Production Branch

on:
  push:
    branches: ['main']

jobs:
  build:
    runs-on: jsonrock

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 24